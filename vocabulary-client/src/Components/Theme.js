import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            light: '#7d3f6d',
            main: '#5d1049',
            dark: '410b33',
        },
        secondary: {
            main: '#fff',
        },
    }
});

export default theme;

